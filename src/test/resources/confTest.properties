############################################################
## Hibernate config.

# hibernate XML file
hibernate.configuration.file=hibernateTest.cfg.xml

############################################################
## Other config.

# character to distinguish elements with the same name 
disambiguatingCharacter=*

# empty character for "label" tag
emptyCharacter=_

############################################################
# build
build.version=junit-build-version
build.timestamp=junit-build-timestamp
build.sha1=junit-build-sha1

############################################################
